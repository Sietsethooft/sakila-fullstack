<section class="client-card">

    <h2 class="mb-4">Film aanmaken</h2>

    <% if (typeof errors !== 'undefined' && errors.length > 0) { %>
        <div class="alert alert-danger">
            <ul>
                <% errors.forEach(function(error) { %>
                    <li><%= error %></li>
                <% }); %>
            </ul>
        </div>
    <% } %>

    <form id="create-movie-form" action="/movieManagement/create" method="POST" novalidate>
    <div class="mb-3">
        <label for="title" class="form-label fw-bold">Titel *</label>
        <input type="text" class="form-control input-field-focus" id="title" name="title" required>
        <div class="invalid-feedback">Titel is verplicht.</div>
    </div>
    <div class="mb-3">
        <label for="description" class="form-label fw-bold">Beschrijving *</label>
        <textarea class="form-control input-field-focus rounded" id="description" name="description" rows="4" required></textarea>
        <div class="invalid-feedback">Beschrijving is verplicht.</div>
    </div>
    <div class="row g-3 mb-3">
        <div class="col-md-6">
            <div class="input-card p-3 shadow-sm rounded">
                <label for="release_year" class="form-label fw-bold">Jaar</label>
                <input type="number" class="form-control input-field-focus" id="release_year" name="release_year" min="1900" max="2099">
            </div>
        </div>
        <div class="col-md-6">
            <div class="input-card p-3 shadow-sm rounded">
                <label for="language" class="form-label fw-bold">Taal *</label>
                <input type="text" class="form-control input-field-focus" id="language" name="language_name" required>
                <div class="invalid-feedback">Taal is verplicht.</div>
            </div>
        </div>
    </div>
    <div class="row g-3 mb-3">
        <div class="col-md-6">
            <div class="input-card p-3 shadow-sm rounded">
                <label for="category" class="form-label fw-bold">Categorie *</label>
                <select class="form-control input-field-focus" id="category" name="category_id" required>
                    <option value="">Selecteer categorie</option>
                    <% categories.forEach(function(category) { %>
                        <option value="<%= category.category_id %>"><%= category.name %></option>
                    <% }); %>
                </select>
                <div class="invalid-feedback">Categorie is verplicht.</div>
            </div>
        </div>
        <div class="col-md-6">
            <div class="input-card p-3 shadow-sm rounded">
                <label for="rating" class="form-label fw-bold">Rating *</label>
                <select class="form-control input-field-focus" id="rating" name="rating" required>
                    <option value="">Selecteer rating</option>
                    <% ratings.forEach(function(rating) { %>
                        <option value="<%= rating %>"><%= rating %></option>
                    <% }); %>
                </select>
            </div>
        </div>
    </div>
    <div class="row g-3 mb-3">
        <div class="col-md-6">
            <div class="input-card p-3 shadow-sm rounded">
                <label for="rental_duration" class="form-label fw-bold">Verhuurduur (dagen) *</label>
                <input type="number" class="form-control input-field-focus" id="rental_duration" name="rental_duration" required min="1">
                <div class="invalid-feedback">Verhuurduur is verplicht.</div>
            </div>
        </div>
        <div class="col-md-6">
            <div class="input-card p-3 shadow-sm rounded">
                <label for="rental_rate" class="form-label fw-bold">Verhuurprijs (€) *</label>
                <input type="number" step="0.01" class="form-control input-field-focus" id="rental_rate" name="rental_rate" required min="0">
                <div class="invalid-feedback">Verhuurprijs is verplicht.</div>
            </div>
        </div>
    </div>
    <div class="row g-3 mb-3">
        <div class="col-md-6">
            <div class="input-card p-3 shadow-sm rounded">
                <label for="length" class="form-label fw-bold">Duur (minuten)</label>
                <input type="number" class="form-control input-field-focus" id="length" name="length" min="1">
            </div>
        </div>
        <div class="col-md-6">
            <div class="input-card p-3 shadow-sm rounded">
                <label for="inventory" class="form-label fw-bold">Aantal exemplaren *</label>
                <input type="number" class="form-control input-field-focus" id="inventory" name="inventory" required min="1">
                <div class="invalid-feedback">Aantal exemplaren is verplicht.</div>
            </div>
        </div>
    </div>
    <button type="submit" class="btn btn-primary px-4">Opslaan</button>
</form>
</section>